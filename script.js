const byteSize = (str) => {
	if(str==''){
		return 0;
	}else{
		let x =str.length;
    return x;
	}
	
	
  // write your code herere
	
};

// Do not change the code below
const str = prompt("Enter some string.");
//alert(byteSize(str));
